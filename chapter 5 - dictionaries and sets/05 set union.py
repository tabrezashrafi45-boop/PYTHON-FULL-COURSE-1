s = {3, 4 ,5 ,6 ,7 }
w = {90,80, 97,4 }

print(s.union(w))
 # union() is used for combine two sets without repeated element
 
print(s.intersection(w))
 # intersection() is used for finding the common value from the sets
 