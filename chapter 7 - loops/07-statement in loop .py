# "BREAK" = is used for exit the loop 
for i in range (345):
    if(i == 23): # uses "if " stattement in break
        break
    print(i) 

    # "continue" is used for skip that value in a loop

    for i in range (5):
        if(i == 3 ): # skip the 3 
            continue
        print(i)

        # "pass" is used to doing nothing with program
        
