'''
File I/O in Python stands for File Input/Output. It is the process of reading data 
from a file (input) and writing data to a file (output).

Instead of storing data only in memory while a program runs, file I/O lets you save data
 permanently on your computer.

 
  # Use open(filename, mode) to open a file.

  # Common modes:

"r" → Read
"w" → Write (overwrites)
"a" → Append

Use read(), readline(), or readlines() to read data.
Use write() to write data.

 '''